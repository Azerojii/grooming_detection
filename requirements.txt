# Core fine-tuning stack
trl>=0.7.0
transformers>=4.40
peft>=0.10.0
accelerate>=0.26.0
datasets>=2.18.0
bitsandbytes>=0.43.0

# App
gradio>=4.0

# XML parsing
lxml

# Optional: GGUF inference backend (CPU/GPU via llama.cpp)
# Uncomment if you want to run the exported GGUF model directly:
# llama-cpp-python
